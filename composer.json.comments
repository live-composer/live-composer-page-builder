
"config": { "platform": { "php": "5.6" – This part needed to limit composer
updates only to the packages that support at least php 5.6. Otherwise our tests
can't run on older php versions due to newer package version requirements.


